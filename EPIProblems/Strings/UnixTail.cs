namespace EPI.Strings
{
    /// <summary>
    /// The unix tail command displays the last part of a file.
    /// Assume tail takes two arguments, a filename and the number of lines starting from 
    /// the last line that are to be printed. Implement the tail command
    /// </summary>
    public static class UnixTail
	{
		public static string Tail(string filename, int numberOfLines)
		{
			return "";
		}
	}
}
